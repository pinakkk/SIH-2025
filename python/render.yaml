services:
  - type: web
    name: hazard-ranking-api
    runtime: python
    plan: free
    autoDeploy: true   # ✅ enable auto-deploy on git push
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
